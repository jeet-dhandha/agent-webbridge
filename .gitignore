node_modules/
*.log
.DS_Store

# Chrome for Testing — downloaded locally for the live test harness (~500MB), never committed
chrome/

# packed extension zips for the GitHub Release (build artifact)
dist/

# runtime state (per-machine; never committed)
run/
state/
*.pid

# demo GIF generator (experiment locally; commit only the built docs/demo.gif)
demo/

# go-to-market drafts / playbooks — local-only, kept out of the public repo + Pages site
docs/LAUNCH-POSTS.md
docs/PUBLISHING.md
docs/SEO-GEO.md
docs/SEO-POSTS.md

# local live-test / scrape harnesses — hardcode local absolute paths, never committed
scratch/
build-next-reality-check.html
